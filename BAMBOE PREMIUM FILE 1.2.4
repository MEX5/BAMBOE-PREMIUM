-- Roblox Lua script to kick a player with a message

-- Function to kick the player with a custom message
function kickPlayerWithMessage(player)
    -- Check if the player is valid
    if player and player:IsA("Player") then
        -- Kick the player with a message
        player:Kick("DEZE KANKER SUKKEL PROBEERDE BAMBOE PREMIUM TE GEBRUIKEN EN IS GECATCHED KANKER HOERTJE.")
    end
end

-- Execute the function to kick the player
kickPlayerWithMessage(game.Players.LocalPlayer)
